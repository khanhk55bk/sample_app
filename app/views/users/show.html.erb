<% provide(:title, @user.name) %>

<div class="row">
	<aside class="span4">
		<section>
			<h1>
				<%= gravatar_for @user, size: 250 %>
				<%= @user.name %> <br/>
			</h1>
		</section>
	</aside>
</div>
